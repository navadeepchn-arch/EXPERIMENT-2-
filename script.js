function loadExp(exp) {
    const frame = document.getElementById("frame");

    if (exp === '2.1') frame.src = "../FSD 2.1/word counter.html";
    else if (exp === '2.2') frame.src = "../FSD 2.2/product.html";
    else if (exp === '2.3') frame.src = "../FSD 2.3/drawing.html";
}
